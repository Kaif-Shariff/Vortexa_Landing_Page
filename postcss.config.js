module.exports = {
    plugins: [
      require('tailwindcss'),
      require('autoprefixer'), // Add autoprefixer for better cross-browser compatibility
      // Add other PostCSS plugins as needed
    ],
  };
  